<Window x:Class="PersonalLibrary.UI.Views.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalLibrary.UI.Views"
        mc:Ignorable="d"
        Title="Add Book" Width="400" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Add a Book" FontSize="20" FontWeight="SemiBold"/> "
            <TextBlock Text="Add a new book to your library" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" >
            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title}" Margin="0,0,0,12"/>

            <TextBlock Text="Author"/>
            <TextBox Text="{Binding Author}" Margin="0,0,0,12"/>

            <TextBlock Text="ISBN"/>
            <TextBox Text="{Binding ISBN}" Margin="0,0,0,12"/>

            <TextBlock Text="Publication Year"/>
            <TextBox Text="{Binding PublicationYear}" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="80" Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
                <Button Content="Save" Width="80" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
